server {
    listen 80;
    listen [::]:80;

    server_name frontend.project;

    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-Ip $remote_addr;
    }
}
